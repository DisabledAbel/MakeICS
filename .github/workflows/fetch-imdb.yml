name: Fetch IMDb TV Schedules

on:
  schedule:
    - cron: '0 16 * * *' # Daily at 8:00 AM PST / 4:00 PM UTC
  workflow_dispatch:

permissions:
  contents: write

jobs:
  fetch-imdb:
    runs-on: ubuntu-latest
    concurrency:
      group: data-updates-${{ github.workflow }}
      cancel-in-progress: false

    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          token: ${{ secrets.PAT_TOKEN }}
          persist-credentials: true
          fetch-depth: 0

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: npm

      - name: Install dependencies
        run: npm install

      - name: Cache Playwright browsers
        uses: actions/cache@v4
        with:
          path: ~/.cache/ms-playwright
          key: ${{ runner.os }}-playwright-${{ hashFiles('**/package-lock.json') }}
          restore-keys: |
            ${{ runner.os }}-playwright-

      - name: Install Playwright Browsers
        run: npx playwright install chromium --with-deps

      - name: Fetch IMDb TV Schedules
        run: node scripts/fetch-imdb.js

      - name: Commit and push changes
        env:
          REF_NAME: ${{ github.ref_name }}
        run: |
          git config --global user.name "DisabledAbel"
          git config --global user.email "196466003+DisabledAbel@users.noreply.github.com"

          git add lib/data/tv/*.json 2>/dev/null || true

          if git diff --cached --quiet; then
            echo "No changes to commit."
            exit 0
          fi

          git commit -m "📺 Update IMDb TV schedules 📺"
          git pull --rebase origin "$REF_NAME"
          git push origin HEAD:"$REF_NAME"
